# required fields
log_level: debug
api_key: APIrPAgFSy37s5g
api_secret: PG93VnGw6VfIpXFIRKu26h0WtVoHwuf8i7ZZ87DAf1GB
ws_url: wss://atl5d-79f5a1jx.livekit.cloud
redis:
  address: redis:6379
  username: ""
  password: ""
  db: 0

# optional fields
#health_port: if used, will open an http port for health checks
#template_port: port used to host default templates (default 7980)
#prometheus_port: port used to collect prometheus metrics. Used for autoscaling
#log_level: debug, info, warn, or error (default info)
#template_base: can be used to host custom templates (default http://localhost:<template_port>/)
#enable_chrome_sandbox: if true, egress will run Chrome with sandboxing enabled. This requires a specific Docker setup, see below.
insecure: true

#s3:
#  access_key: 7L9cPxpjFxbQmfpJM203
#  secret: 5AUwtZV0CBsAfrpyDVccvy75YYIqB4wAeOr9p66Q
#  endpoint: http://minio:9010
#  bucket: nests
#  aws_log_level: debug
  